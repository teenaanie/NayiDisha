import Link from 'next/link';
import {Icon} from './ops/dashboard-icon';
export function WorkspaceIntro({eyebrow,title,description,action}:{eyebrow:string;title:React.ReactNode;description:React.ReactNode;action?:{href:string;label:string}}){return <div className="nd-dashboard-intro"><div><div className="nd-section-kicker"><Icon name="spark"/>{eyebrow}</div><h1>{title}</h1><p>{description}</p></div>{action&&<Link className="btn btn-primary" href={action.href}>+ {action.label}</Link>}</div>;}
export function WorkspaceNextSteps({items,title,description,href,label}:{items:{href:string;label:string;icon:string;tone:string}[];title:string;description:string;href:string;label:string}){return <div className="nd-overview-grid"><section className="card"><div className="card-head"><h2>Quick actions</h2></div><div className="card-body"><div className="nd-shortcuts">{items.map(item=><Link className={`nd-shortcut tone-${item.tone}`} href={item.href} key={item.href}><Icon name={item.icon}/>{item.label}<span aria-hidden="true">→</span></Link>)}</div></div></section><section className="nd-guidance"><Icon name="spark"/><h2>{title}</h2><p>{description}</p><Link href={href}>{label} →</Link></section></div>;}
export function ListToolbar({count,label,query,placeholder='Search records…',action}:{count:number;label:string;query?:string;placeholder?:string;action?:string}){return <div className="nd-page-toolbar"><p><strong>{count}</strong> {label}</p>{action&&<form action={action} className="nd-filter"><label className="nd-sr-only" htmlFor="record-search">Search {label}</label><input id="record-search" name="q" defaultValue={query} placeholder={placeholder}/><button className="btn btn-primary">Search</button>{query&&<Link className="btn" href={action}>Clear</Link>}</form>}</div>;}
export function EmptyState({title,description}:{title:string;description:string}){return <div className="empty"><Icon name="shield"/><h3 style={{margin:'10px 0 6px'}}>{title}</h3><p>{description}</p></div>;}
