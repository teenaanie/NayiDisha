import {sql} from '@/lib/db';import {requireRole} from '@/lib/auth';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const actor=await requireRole(['CANDIDATE','ADMIN']);const {id}=await params;const [r]=await sql`SELECT response FROM app.data_request WHERE id=${id} AND kind='ACCESS' AND status='ACTIONED' AND (${actor.role==='ADMIN'} OR candidate_id=${actor.id})`;if(!r)return new Response('Not found',{status:404});return new Response(JSON.stringify(r.response,null,2),{headers:{'content-type':'application/json','content-disposition':'attachment; filename="my-data.json"','cache-control':'no-store'}});}
