 'use client';
import {useState,useTransition} from 'react';import {confirm} from './journey-actions';
export function Reconfirm({id,title}:{id:string;title:string}){const [pending,start]=useTransition(),[msg,set]=useState('');return <div className="note"><p>{title}: please reconfirm your interest before the employer can unlock your profile.</p><button className="btn btn-primary" disabled={pending} onClick={()=>start(async()=>{try{const r=await confirm(id);set(r.qualified?'Confirmed and qualified.':'Confirmed. Remaining requirements: '+r.gaps.join(', '));}catch(e){set(e instanceof Error?e.message:'Failed');}})}>Reconfirm interest</button><p role="status">{msg}</p></div>}
